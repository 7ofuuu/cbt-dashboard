NEXT_PUBLIC_HOST=http://localhost:3000/api/
# Optional: backend URL via ngrok, used when the dashboard is opened through an
# ngrok URL. Set to your reserved ngrok static domain (see ngrok.yml).
NEXT_PUBLIC_HOST_NGROK=https://cbt-be.ngrok-free.app/api/
